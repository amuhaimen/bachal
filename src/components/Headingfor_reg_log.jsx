import { ClassNames } from "@emotion/react";
import React from "react";

const Headingfor_reg_log = ({ title, className }) => {
  return <h3 className={className}>{title}</h3>;
};

export default Headingfor_reg_log;
